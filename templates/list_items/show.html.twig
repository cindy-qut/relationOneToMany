{% extends 'base.html.twig' %}

{% block title %}ListItems{% endblock %}

{% block body %}
    <h1>ListItems</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ list_item.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ list_item.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('list_items_index') }}">back to list</a>

    <a href="{{ path('list_items_edit', {'id': list_item.id}) }}">edit</a>

    {{ include('list_items/_delete_form.html.twig') }}
{% endblock %}
